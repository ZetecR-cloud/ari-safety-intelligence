export const metadata = {
  title: "ARI Safety Intelligence",
  description: "ARI Safety Intelligence",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
